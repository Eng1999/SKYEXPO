import { WorkGallery } from "@/components/work/WorkGallery";

export const metadata = {
  title: "Work — Sky Expo",
  description: "SKY EXPO portfolio — real events, exhibitions, and productions.",
};

export default function WorkPage() {
  return <WorkGallery />;
}
