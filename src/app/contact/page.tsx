import { ContactSection } from "@/components/contact/ContactSection";

export const metadata = {
  title: "Connect — Sky Expo",
};

export default function ContactPage() {
  return <ContactSection />;
}
